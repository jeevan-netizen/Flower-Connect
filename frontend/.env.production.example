# Production environment configuration for FlowerConnect frontend
# Copy this to .env.production (or set in your deployment platform) and fill in real values

# API base URL — the backend's production API endpoint
VITE_API_BASE_URL=https://api.flowerconnect.com/api/v1
